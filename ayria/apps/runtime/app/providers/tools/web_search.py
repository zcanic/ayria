"""Web search tool placeholder.

This should remain optional and policy-controlled.
If cloud search is used later, make sure privacy and consent rules are enforced.
"""

async def web_search(query: str) -> dict:
    return {'query': query, 'results': []}
