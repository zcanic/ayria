"""File reader tool placeholder.

Do not allow arbitrary silent file reads from the model.
This tool should later check allowlists, user confirmation, or explicit scopes.
"""

async def read_file(path: str) -> dict:
    return {'path': path, 'content': ''}
