"""Persona rewriter agent placeholder.

Use this only after the capability result exists.
That order is the key design decision that keeps tool use stable.
"""

class PersonaRewriterAgent:
    async def run(self, text: str) -> str:
        return text
